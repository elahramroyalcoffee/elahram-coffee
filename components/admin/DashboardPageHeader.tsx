"use client";
import React from "react";

function DashboardPageHeader({ title }: { title: string }) {
  return <div className="dashboard-page-header">{title}</div>;
}

export default DashboardPageHeader;
